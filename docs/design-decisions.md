# Design Decisions — SciLibrarian

A running record of architectural choices and the reasoning behind them. Intended as a living document updated each development cycle.

---

## Naming: "Alexandria"

**Decision:** Name the librarian agent Alexandria.

**Rationale:** The Library of Alexandria was the ancient world's foremost attempt at comprehensive knowledge collection — which is precisely what this system does for AI safety literature. The name is immediately understood, scholarly, and memorable. "Alex" works as an informal shorthand in UI text.

---

## Stack choice: FastAPI + PostgreSQL + React

**Decision:** Python backend (FastAPI) with PostgreSQL and a React frontend.

**Rationale:**
- The Anthropic Python SDK is the primary SDK and has the best support
- FastAPI's async support is important for streaming Claude responses and parallel ingestion tasks
- PostgreSQL gives us full-text search for free (no additional vector store needed in Cycle 1), and pgvector extension makes upgrading to semantic search a schema migration rather than a rewrite
- React + Vite + Tailwind is the most practical combination for a quickly iterable, good-looking UI

**Alternative considered:** A Python monolith (FastAPI serving Jinja2 templates). Rejected because the Alexandria chat panel needs WebSocket or streaming, which is better handled by a separate React component.

---

## Multi-project from day one

**Decision:** Top-level Project model from Cycle 1, not retrofitted later.

**Rationale:** Research groups often run parallel projects (e.g., alignment theory vs. governance policy). Adding project isolation later would require a migration that touches every table. The cost of adding it now is low (one extra FK in collections and references). Projects also serve as the natural scope for digests and watch monitors.

---

## Full-text search first, vector search later

**Decision:** Start with PostgreSQL `LIKE`-based full-text search; upgrade to pgvector in Cycle 3.

**Rationale:** For a Cycle 1 prototype, the query load is low and the corpus is small. PostgreSQL FTS is already good enough to find papers by keyword. The cost of setting up and maintaining a separate vector store (Qdrant, Chroma) is not justified until we have evidence that keyword search is insufficient. pgvector keeps everything in one database.

---

## Alexandria uses tool use for library search

**Decision:** The librarian chat uses Claude's tool use (function calling) to search the library before responding.

**Rationale:** This is the correct pattern for RAG (retrieval-augmented generation). Without tool use, we'd have to decide up-front how much context to inject into the prompt. With tool use, Alexandria decides when and what to search — she can make multiple searches with different queries, or skip searching if the question is clearly general. The tool use loop is simple (single tool: `search_library`) so latency is acceptable.

---

## Human review queue as a first-class concept

**Decision:** Proactive search results always go through a review queue; they are never auto-committed to the library.

**Rationale:** Automated search (arXiv, Semantic Scholar) returns a lot of noise. For an AI safety reference library, quality over quantity matters. A false positive (an irrelevant paper auto-added) erodes trust in the library. The review queue also creates a natural workflow: researchers can triage in batch each week.

---

## CLI yes, TUI no (for now)

**Decision:** Build a Typer-based CLI. Defer TUI (terminal UI using Textual or Rich).

**Rationale:** A CLI is immediately useful for batch ingestion (dropping a directory of PDFs), scripting, and automation. A TUI adds considerable complexity for uncertain payoff — the web UI already provides a good interactive experience. If researchers express a strong preference for terminal-only workflows, a TUI can be added in Cycle 2 or 3.

**CLI scope:** login, ingest (PDF or URL), ingest-dir (batch), search, digest. Enough to be useful without duplicating everything in the web UI.

---

## Metadata generated by Claude, not heuristics

**Decision:** Claude generates all metadata (title, authors, year, source_type, abstract, summary, tags) from the extracted text.

**Rationale:** PDF metadata fields are notoriously unreliable (wrong titles, missing authors, garbled abstracts). For government PDFs and policy documents, there is often no standard metadata at all. Claude produces consistently structured, well-formed metadata regardless of input format. The cost per ingestion is acceptable (one API call per document).

**Trade-off:** Ingestion is slower and incurs API cost. Mitigated by model selection (Haiku is available for bulk ingestion where speed matters more than quality).

---

## Project onboarding generates initial collection structure

**Decision:** On project creation, Alexandria analyses the project description and generates a suggested collection taxonomy, which is immediately instantiated as real collections in the database.

**Rationale:** Getting started with an empty library is daunting. By generating a structure upfront, we give users a scaffold they can immediately use and modify. The structure is based on the actual research domain, so it's relevant rather than generic. Restructure suggestions (calling `suggest_restructure` as the library grows) extend this philosophy: Alexandria is always aware of how the library is evolving.

---

## Monthly digest format

**Decision:** Digest is generated as a structured Markdown document covering: new additions, state of the art by topic, notable developments, coverage gaps, and recommended next steps.

**Rationale:** The digest is meant to be read in full, not just skimmed. Structured sections help different readers extract what they need. "Coverage gaps" and "recommended next steps" are specifically designed to close the loop between the digest and the monitor/watch-request system — Alexandria identifies gaps, users can create monitors to fill them.

---

## Library organisation for research teams

**Decision:** Project → Collections (hierarchical folders) → References. One library per project.

**Rationale:** Research teams often run parallel workstreams. A "project" scopes everything — the library, monitors, digests, and Alexandria's configuration — so teams can maintain truly separate reference sets without cross-contamination. Within a project, collections provide a folder hierarchy that Alexandria designs on creation and can suggest reorganising as the library grows.

**Key insight:** Collections are the primary navigation. The sidebar collection tree drives what's shown in the Library view. "Library" (flat search) and "Collections" (tree management) are two views of the same data, not separate concepts.

**What researchers actually need beyond basic organisation:**
- Tags as a second axis (orthogonal to folders) — a paper can be in "Technical AI Safety" AND tagged "interpretability, sparse-autoencoder"
- Read/starred status — track what you've actually read vs filed
- Notes/annotations — personal context on why a paper matters
- Bulk operations — move batches of references between collections
- Export — BibTeX, Zotero RDF, or plain citation list

These are all planned for future cycles.

## Authentication: JWT, no refresh tokens yet

**Decision:** Simple JWT with 24-hour expiry. No refresh token flow in Cycle 1.

**Rationale:** This is an internal tool for a research group, not a public-facing application. A 24-hour token expiry is acceptable; users can just log in again. Implementing refresh tokens properly (secure httpOnly cookies, rotation) adds complexity that isn't justified for the current user base. This should be revisited before a public deployment.

---

## Uploads stored on filesystem, metadata in PostgreSQL

**Decision:** PDF binary files stored in the Docker volume at `/app/uploads/`. Only the file path (and extracted text/metadata) stored in PostgreSQL.

**Rationale:** Storing BLOBs in PostgreSQL is generally discouraged for large files — it bloats the database, complicates backups, and slows queries. The filesystem approach is simpler and easier to migrate to object storage (S3, GCS) in Cycle 3. The extracted text and metadata are in the DB for searchability.

**Git policy:** `uploads/` is in `.gitignore`. The metadata (title, summary, tags) is version-controlled via the database. For a reproducible corpus, users should track the original source URLs (which Alexandria extracts).
